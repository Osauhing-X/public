# Workspace settings

Settings manage portal details, user access, integrations, plan and domain. Adding a custom domain requires an active plan; without DNS, routing uses the portal’s internal identifier. Ending a service must use the persistent portal identifier and must not assume a domain exists.
