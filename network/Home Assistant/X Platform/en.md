# X Platform

X Platform discovers public and private GitHub repositories, installs dependencies, builds and runs multiple applications. Its console manages ports, environment variables, updates, processes and logs. Integrations are copied to Home Assistant `custom_components`, while persistent settings, repositories and logs stay under the add-on `/data` directory. Install trusted source code only because managed applications inherit the Home Assistant runtime token.
