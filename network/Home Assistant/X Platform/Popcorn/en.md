# Popcorn

Popcorn runs as an application managed by X Platform. Personal saves remain in browser storage, while shared family choices persist under the add-on `/data` directory. The application can integrate Home Assistant notifications, people, lights and switches.
