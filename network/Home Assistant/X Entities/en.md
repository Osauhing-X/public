# X Entities

X Entities discovers compatible Node.js applications through Zeroconf, accepts state snapshots and creates dynamic Home Assistant entities. Applications publish state to the integration API, while switch and button commands return to the application `/update` endpoint. Stale sources become unavailable and are eventually removed.
